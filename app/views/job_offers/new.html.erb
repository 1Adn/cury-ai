<h1>New Job Offer</h1>

<%= simple_form_for @job_offer do |f| %>
  <%= f.input :title %>
  <%= f.input :description %>
  <%= f.button :submit, "Save job offer !", class: 'btn btn-primary' %>
<% end %>
